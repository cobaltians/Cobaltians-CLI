#!/usr/bin/env node

/**
 * Module dependencies.
 */

var program = require('commander');

program
    .version('0.5.0')
    .command('create appName templateName', 'Create an cobalt app from a template')
    .command('upgrade', 'Upgrade Cobalt')
    .parse(process.argv);